{
  "meta": {
    "title": "Python Operators Explained | Arithmetic, Logical, Bitwise & More 2026 | Tech Sustainify",
    "description": "A complete, no-fluff guide to Python operators — arithmetic, comparison, assignment, logical, bitwise, membership, and identity operators. Real bugs, real error messages, precedence rules, and the gotchas that actually bite you in production.",
    "keywords": "python operators, arithmetic operators python, comparison operators python, logical operators python, bitwise operators python, assignment operators python, membership operators python, identity operators python, python operator precedence, walrus operator python, python is vs ==, floor division python, python interview questions operators",
    "author": "Tech Sustainify",
    "canonical": "https://techsustainify.com/python/python-operators",
    "ogTitle": "Python Operators Explained — Arithmetic, Logical, Bitwise & More | Tech Sustainify",
    "ogDescription": "Every Python operator explained with real bugs and real error messages — not textbook definitions. Arithmetic, comparison, logical, bitwise, membership, identity, precedence, and the walrus operator.",
    "ogUrl": "https://techsustainify.com/python/python-operators",
    "ogImage": "https://techsustainify.com/images/python-operators-og.png",
    "ogSiteName": "Tech Sustainify",
    "twitterReadingTime": "28 minutes",
    "twitterLevel": "Beginner to Intermediate",
    "schemaDatePublished": "2024-02-10",
    "schemaDateModified": "2026-07-21",
    "schemaProficiencyLevel": "Beginner",
    "schemaWordCount": 8100
  },

  "breadcrumbs": [
    { "position": 1, "name": "Home",            "path": "/" },
    { "position": 2, "name": "Python Tutorial", "path": "/pythonhome" },
    { "position": 3, "name": "Python Operators" }
  ],

  "articleHeader": {
    "badge": "🐍 Python",
    "title": "Python Operators — The Complete Guide",
    "subtitle": "Arithmetic, comparison, assignment, logical, bitwise, membership, and identity operators — explained the way you'd explain them to a teammate at 2am, not the way a textbook does."
  },

  "infoCards": [
    { "icon": "📅", "label": "Last Updated", "isTime": true, "dateTime": "2026-07-21", "value": "July 2026" },
    { "icon": "⏱️", "label": "Read Time",    "value": "28 min" },
    { "icon": "🎯", "label": "Level",         "value": "Beginner to Intermediate" }
  ],

  "sections": [

    {
      "sectionId": "what-is-python-operators",
      "title": "What Are Operators in Python?",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "An <strong>operator</strong> is a symbol that tells Python to perform a specific operation on one or more values. That's the textbook line. Here's the real one: operators are the glue between your variables and the actual logic of your program. Every <code>if</code> statement, every loop condition, every calculation you've ever written leans on operators. You just don't notice them until one of them breaks something at 1 AM and you're staring at a traceback wondering why <code>3 == 3.0</code> is <code>True</code> but <code>3 is 3.0</code> is <code>False</code>."
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Python groups operators into seven families: <strong>arithmetic</strong>, <strong>comparison</strong>, <strong>assignment</strong>, <strong>logical</strong>, <strong>bitwise</strong>, <strong>membership</strong>, and <strong>identity</strong>. Some languages bury half of these behind verbose method calls (looking at you, Java's <code>.equals()</code> vs <code>==</code> confusion). Python just gives you the symbol and expects you to understand the semantics underneath it. That's simpler on the surface and genuinely more dangerous underneath — which is exactly the trade-off this guide is about."
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Here's my honestly opinionated take, and I'll say it upfront so you can disagree with me for the rest of the article: <strong>the operators most Python tutorials rush through — <code>is</code>, chained comparisons, and floor division with negative numbers — are exactly the ones that cause production bugs.</strong> The arithmetic stuff everyone already gets right on day one. It's the quiet, \"obviously this works\" operators that eventually bite you during a code review, or worse, during an on-call incident. We're going to spend disproportionate time on those."
        }
      ]
    },

    {
      "sectionId": "types-of-operators-heading",
      "title": "The 7 Categories of Python Operators",
      "blocks": [
        {
          "type": "paragraph",
          "text": "Before diving into each one individually, here's the full map. Keep this list open in a tab — you'll come back to it more than you expect, especially the last two categories, which almost nobody remembers correctly six months after learning Python."
        },
        {
          "type": "bulletList",
          "items": [
            { "strong": "Arithmetic Operators", "text": " — +, -, *, /, //, %, ** — basic math, plus two operators (// and **) that most other mainstream languages don't even have." },
            { "strong": "Comparison Operators", "text": " — ==, !=, >, <, >=, <= — compare values and return a boolean. Python also lets you chain them, which is genuinely nice syntax." },
            { "strong": "Assignment Operators", "text": " — =, +=, -=, *=, /=, //=, **=, %=, &=, |=, ^=, >>=, <<=, and the newer walrus := — assign or update values." },
            { "strong": "Logical Operators", "text": " — and, or, not — combine boolean expressions using short-circuit evaluation, not the symbols && and || you'd expect from C-family languages." },
            { "strong": "Bitwise Operators", "text": " — &, |, ^, ~, <<, >> — operate on the actual binary representation of integers. Rare in day-to-day scripting, essential in networking, flags, and low-level performance code." },
            { "strong": "Membership Operators", "text": " — in, not in — check whether a value exists inside a sequence (list, tuple, string, set, dict). Deceptively simple, with real performance implications." },
            { "strong": "Identity Operators", "text": " — is, is not — check whether two variables point to the exact same object in memory, not whether they hold equal values. This is the one that trips up almost everyone at least once." }
          ]
        }
      ]
    },

    {
      "sectionId": "arithmetic-operators",
      "title": "Arithmetic Operators — More Than Just + and -",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Python gives you seven arithmetic operators. Five of them are what you'd expect from any calculator. The other two — <code>//</code> (floor division) and <code>**</code> (exponentiation) — are the ones worth slowing down for, because they're where beginners (and honestly, experienced devs switching from Java or C++) get caught off guard."
        },
        {
          "type": "comparisonTable",
          "headers": ["Operator", "Name", "Example", "Result"],
          "rows": [
            ["+", "Addition", "7 + 3", "10"],
            ["-", "Subtraction", "7 - 3", "4"],
            ["*", "Multiplication", "7 * 3", "21"],
            ["/", "True Division", "7 / 3", "2.3333333333333335"],
            ["//", "Floor Division", "7 // 3", "2"],
            ["%", "Modulus", "7 % 3", "1"],
            ["**", "Exponentiation", "7 ** 3", "343"]
          ]
        },
        {
          "type": "subHeading",
          "text": "The Negative Number Trap in Floor Division"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "This is the one that gets people. <code>//</code> doesn't truncate towards zero like integer division in C or Java — it rounds <strong>towards negative infinity</strong>. So <code>7 // 2</code> gives you <code>3</code>, which feels normal. But <code>-7 // 2</code> gives you <code>-4</code>, not <code>-3</code>. I've seen this exact bug in a billing calculation script where someone assumed floor division on negative refund amounts would behave like C's integer division. It didn't, and the invoice was off by a rupee on every single negative-adjustment line item. Nobody noticed for two release cycles."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "floor_division_gotcha.py",
          "lines": [
            "print(7 // 2)     # 3  (as expected)",
            "print(-7 // 2)    # -4 (rounds toward negative infinity, NOT -3)",
            "print(7 // -2)    # -4",
            "print(-7 // -2)   # 3",
            "",
            "# If you actually want truncation toward zero (like C/Java):",
            "import math",
            "print(math.trunc(-7 / 2))   # -3"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["3", "-4", "-4", "3", "-3"]
        },
        {
          "type": "subHeading",
          "text": "True Division Always Returns a Float"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "In Python 3, <code>/</code> always returns a <code>float</code>, even when both operands are integers and divide evenly. <code>10 / 2</code> gives <code>5.0</code>, not <code>5</code>. This bit a lot of teams during Python 2 to 3 migrations — old code that expected <code>int</code> results from division silently started producing floats, and downstream type checks (or worse, <code>isinstance</code> checks feeding into JSON serialization) started behaving differently. If you need an integer result, use <code>//</code> explicitly, or wrap with <code>int()</code> if you specifically want truncation."
        },
        {
          "type": "subHeading",
          "text": "The Modulus Operator With Negative Numbers"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Same story as floor division, because they're mathematically linked (Python guarantees <code>a == (a // b) * b + (a % b)</code>). <code>-7 % 3</code> is <code>2</code> in Python, not <code>-1</code> like you'd get in C. This actually makes Python's modulus more useful for things like wrapping array indices or clock arithmetic — <code>(-1) % 7</code> gives you <code>6</code>, which is exactly the \"one day before day 0\" behavior you want when working with days-of-week logic. It's a feature once you understand it, a nasty surprise if you don't."
        },
        {
          "type": "subHeading",
          "text": "Exponentiation — ** vs pow() vs math.pow()"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "There are actually three ways to raise a number to a power in Python, and they're not interchangeable. <code>2 ** 10</code> gives you the integer <code>1024</code>. The built-in <code>pow(2, 10)</code> does the same thing but also supports a third argument for modular exponentiation — <code>pow(2, 10, 1000)</code> gives <code>24</code>, computing <code>(2**10) % 1000</code> efficiently without ever materializing the full number. That third-argument form is genuinely useful in cryptography code (RSA implementations lean on it heavily). <code>math.pow()</code>, on the other hand, always returns a <code>float</code>, even for integer inputs — <code>math.pow(2, 10)</code> gives <code>1024.0</code>. Mixing these up in code that expects exact integers has caused more than one subtle rounding bug in scientific computing scripts I've reviewed."
        }
      ]
    },

    {
      "sectionId": "comparison-operators",
      "title": "Comparison Operators — Chaining and the Type Mismatch Trap",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Comparison operators return a boolean — <code>True</code> or <code>False</code>. There are six of them: <code>==</code>, <code>!=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>. Simple on paper. Where Python gets genuinely clever — and where a lot of other languages fall short — is chaining."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "chained_comparison.py",
          "lines": [
            "age = 25",
            "",
            "# Instead of this (which every C/Java developer writes out of habit):",
            "if age >= 18 and age <= 60:",
            "    print(\"Eligible\")",
            "",
            "# Python lets you write this instead:",
            "if 18 <= age <= 60:",
            "    print(\"Eligible\")"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["Eligible", "Eligible"]
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Both blocks print the same thing, but the chained version is shorter, reads closer to actual math notation, and — this part matters — evaluates <code>age</code> only once instead of twice. If <code>age</code> were a function call with side effects (say, a database read), the chained form calls it once; the <code>and</code> form calls it twice. Small detail, real difference in code that hits an API or a DB inside the condition."
        },
        {
          "type": "subHeading",
          "text": "Comparing Different Types — When Python Refuses"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Python 3 is stricter here than Python 2 ever was. In Python 2, comparing a string and an integer would silently give you some arbitrary (but consistent) ordering. In Python 3, it just refuses. Try <code>'5' &gt; 3</code> and you'll get <code>TypeError: '&gt;' not supported between instances of 'str' and 'int'</code>. I actually think this is the right call by the language designers — silent comparisons between incompatible types are exactly the kind of thing that hides real bugs behind a passing test suite. Equality is the one exception: <code>'5' == 5</code> doesn't raise an error, it just quietly returns <code>False</code>, because <code>==</code> falls back to identity-based inequality when types can't be meaningfully compared."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "type_comparison_error.py",
          "lines": [
            "print('5' == 5)     # False — no error, just not equal",
            "print('5' > 5)      # TypeError: '>' not supported between instances of 'str' and 'int'"
          ]
        },
        {
          "type": "outputBox",
          "lines": [
            "False",
            "Traceback (most recent call last):",
            "TypeError: '>' not supported between instances of 'str' and 'int'"
          ]
        },
        {
          "type": "subHeading",
          "text": "Comparing Floats — The 0.1 + 0.2 Problem"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "This isn't a Python bug, it's IEEE 754 floating-point representation — the same issue exists in JavaScript, Java, C, basically everywhere floats are binary. <code>0.1 + 0.2 == 0.3</code> evaluates to <code>False</code> because <code>0.1 + 0.2</code> actually equals <code>0.30000000000000004</code> under the hood. If you're comparing floats for equality anywhere near money or measurements, use <code>math.isclose()</code> instead of <code>==</code>, or better, use the <code>decimal</code> module for anything financial. I've seen this exact comparison break a discount-calculation unit test in a way that took the whole team almost a full day to track down, because the test data happened to hit one of the rare cases where the rounding error surfaced."
        }
      ]
    },

    {
      "sectionId": "assignment-operators",
      "title": "Assignment Operators — Including the Walrus",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "The plain <code>=</code> operator assigns a value to a variable. The compound assignment operators — <code>+=</code>, <code>-=</code>, <code>*=</code>, <code>/=</code>, <code>//=</code>, <code>**=</code>, <code>%=</code>, plus the bitwise versions <code>&=</code>, <code>|=</code>, <code>^=</code>, <code>>>=</code>, <code>&lt;&lt;=</code> — are shorthand for \"take the current value, do something to it, store the result back.\" Nothing exotic. The one that actually deserves its own section is the walrus operator, <code>:=</code>, added in Python 3.8."
        },
        {
          "type": "comparisonTable",
          "headers": ["Operator", "Equivalent To", "Example"],
          "rows": [
            ["x += 5", "x = x + 5", "count += 1"],
            ["x -= 5", "x = x - 5", "balance -= amount"],
            ["x *= 5", "x = x * 5", "price *= 1.18"],
            ["x /= 5", "x = x / 5", "avg /= total"],
            ["x //= 5", "x = x // 5", "page //= 10"],
            ["x **= 5", "x = x ** 5", "value **= 2"],
            ["x %= 5", "x = x % 5", "index %= len(arr)"]
          ]
        },
        {
          "type": "subHeading",
          "text": "The Walrus Operator (:=)"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "The walrus operator lets you assign a value <em>and</em> use it in the same expression. Before Python 3.8, you'd write something like this to avoid calling a function twice inside a while loop:"
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "before_walrus.py",
          "lines": [
            "data = input(\"Enter value: \")",
            "while data != \"quit\":",
            "    print(f\"You entered: {data}\")",
            "    data = input(\"Enter value: \")"
          ]
        },
        {
          "type": "paragraph",
          "text": "With the walrus operator, the same logic reads cleaner and avoids repeating the input() call:"
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "after_walrus.py",
          "lines": [
            "while (data := input(\"Enter value: \")) != \"quit\":",
            "    print(f\"You entered: {data}\")"
          ]
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "It's also great inside list comprehensions when you need to reuse an expensive computation without calling it twice: <code>[y for x in data if (y := expensive_call(x)) > 0]</code>. My honest opinion — use the walrus operator sparingly. It reads great in the two or three patterns it was designed for (while-loops, comprehensions with a filtered computed value) and reads terribly everywhere else. I've seen people cram it into places where a plain two-line assignment would have been clearer, purely because it felt like \"modern Python.\" Don't do that."
        },
        {
          "type": "subHeading",
          "text": "The += Mutation Trap on Lists"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Here's a gotcha that's genuinely underrated. For immutable types like integers and strings, <code>x += 1</code> and <code>x = x + 1</code> behave identically. But for mutable types like lists, they do NOT behave the same way. <code>list_a += [4]</code> mutates the list in place (calls <code>__iadd__</code>). <code>list_a = list_a + [4]</code> creates a brand new list object. If another variable is pointing at the same list, the in-place version changes what that other variable sees too — the reassignment version doesn't."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "plus_equals_mutation.py",
          "lines": [
            "a = [1, 2, 3]",
            "b = a          # b points to the SAME list object as a",
            "",
            "a += [4]       # mutates in place",
            "print(b)       # [1, 2, 3, 4]  <- b changed too, surprise!",
            "",
            "a = [1, 2, 3]",
            "b = a",
            "a = a + [4]    # creates a NEW list, a now points elsewhere",
            "print(b)       # [1, 2, 3]  <- b is unaffected"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["[1, 2, 3, 4]", "[1, 2, 3]"]
        }
      ]
    },

    {
      "sectionId": "logical-operators",
      "title": "Logical Operators — and, or, not (Not && and ||)",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "If you're coming from JavaScript, Java, or C, the first thing to unlearn is reaching for <code>&&</code> and <code>||</code>. Python spells them out: <code>and</code>, <code>or</code>, <code>not</code>. This is a deliberate readability choice, and honestly, once you get used to it, going back to symbol soup in other languages feels unnecessarily cryptic."
        },
        {
          "type": "bulletList",
          "items": [
            { "strong": "and", "text": " — returns True only if both operands are truthy. Short-circuits: if the left side is falsy, Python never evaluates the right side." },
            { "strong": "or", "text": " — returns True if either operand is truthy. Short-circuits the other way: if the left side is truthy, the right side is never evaluated." },
            { "strong": "not", "text": " — flips a boolean. not True is False, not 0 is True (because 0 is falsy)." }
          ]
        },
        {
          "type": "subHeading",
          "text": "Short-Circuit Evaluation Isn't Just an Optimization"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Short-circuiting is used deliberately as a control-flow pattern, not just as a speed trick. The classic example: <code>user and user.profile and user.profile.email</code>. If <code>user</code> is <code>None</code>, Python never touches <code>.profile</code>, so you avoid an <code>AttributeError: 'NoneType' object has no attribute 'profile'</code>. This pattern shows up constantly in Django templates and view code, especially when dealing with optional foreign key relationships pulled from a database that might return null."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "short_circuit.py",
          "lines": [
            "user = None",
            "",
            "# This would crash:",
            "# email = user.profile.email",
            "",
            "# This is safe because 'and' short-circuits at the first falsy value:",
            "email = user and user.profile and user.profile.email",
            "print(email)   # None — no crash"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["None"]
        },
        {
          "type": "subHeading",
          "text": "Logical Operators Don't Always Return a Boolean"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "This one surprises people who assume <code>and</code>/<code>or</code> always return <code>True</code>/<code>False</code> like in most other languages. They don't — they return one of the actual operands. <code>and</code> returns the first falsy operand, or the last one if all are truthy. <code>or</code> returns the first truthy operand, or the last one if all are falsy. This is exactly why the common pattern <code>value = user_input or \"default\"</code> works — if <code>user_input</code> is an empty string (falsy), you get <code>\"default\"</code> back; otherwise you get <code>user_input</code> itself, not a boolean."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "logical_returns_value.py",
          "lines": [
            "print(0 or \"fallback\")     # 'fallback' (0 is falsy)",
            "print(\"hi\" or \"fallback\")  # 'hi' (first truthy value)",
            "print(3 and \"done\")        # 'done' (3 is truthy, returns second operand)",
            "print(0 and \"done\")        # 0 (short-circuits, returns first falsy operand)"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["fallback", "hi", "done", "0"]
        }
      ]
    },

    {
      "sectionId": "bitwise-operators",
      "title": "Bitwise Operators — Where They Actually Matter",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Most Python developers go months, sometimes years, without touching bitwise operators. Then one day you're implementing permission flags, working with a binary protocol, doing image processing with raw pixel data, or writing something performance-sensitive, and suddenly <code>&</code>, <code>|</code>, <code>^</code>, <code>~</code>, <code>&lt;&lt;</code>, and <code>&gt;&gt;</code> become essential. They work directly on the binary representation of integers."
        },
        {
          "type": "comparisonTable",
          "headers": ["Operator", "Name", "Example", "Binary Explanation"],
          "rows": [
            ["&", "AND", "12 & 10", "1100 & 1010 = 1000 (8)"],
            ["|", "OR", "12 | 10", "1100 | 1010 = 1110 (14)"],
            ["^", "XOR", "12 ^ 10", "1100 ^ 1010 = 0110 (6)"],
            ["~", "NOT (invert)", "~12", "-(12+1) = -13"],
            ["<<", "Left Shift", "3 << 2", "0011 → 1100 = 12 (multiply by 4)"],
            [">>", "Right Shift", "12 >> 2", "1100 → 0011 = 3 (divide by 4)"]
          ]
        },
        {
          "type": "subHeading",
          "text": "Real Use Case: Permission Flags"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "One place bitwise operators earn their keep in real code: representing multiple boolean flags in a single integer, instead of a bunch of separate boolean columns in a database. Unix file permissions work exactly this way — that's why <code>chmod 755</code> makes sense once you know it's three groups of 3 bits each. Here's a simplified permission-flag system you might actually write for a role-based access system."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "permission_flags.py",
          "lines": [
            "READ = 1      # 001",
            "WRITE = 2     # 010",
            "EXECUTE = 4   # 100",
            "",
            "user_permissions = READ | WRITE   # combine flags: 011 = 3",
            "",
            "# Check if a flag is set using AND",
            "can_write = bool(user_permissions & WRITE)",
            "print(can_write)          # True",
            "",
            "can_execute = bool(user_permissions & EXECUTE)",
            "print(can_execute)        # False",
            "",
            "# Add EXECUTE permission using OR",
            "user_permissions |= EXECUTE",
            "print(user_permissions)   # 7 (111 — all three flags now set)",
            "",
            "# Remove WRITE permission using XOR",
            "user_permissions ^= WRITE",
            "print(user_permissions)   # 5 (101 — WRITE removed)"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["True", "False", "7", "5"]
        },
        {
          "type": "subHeading",
          "text": "Shift Operators for Fast Power-of-2 Math"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "<code>x &lt;&lt; n</code> is equivalent to <code>x * (2 ** n)</code>, and <code>x &gt;&gt; n</code> is equivalent to <code>x // (2 ** n)</code>, but computed directly at the bit level, which is meaningfully faster for large numbers or tight loops. You'll see this in hashing implementations, compression algorithms, and low-level networking code (packing/unpacking bytes for a socket protocol). For everyday application code, don't reach for shift operators just to look clever — write <code>x * 2</code> if that's what you mean. Readability wins unless you're in a genuinely hot code path."
        }
      ]
    },

    {
      "sectionId": "membership-operators",
      "title": "Membership Operators — in and not in",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "<code>in</code> and <code>not in</code> check whether a value exists within a sequence — a list, tuple, string, set, or the keys of a dict. Syntactically trivial. Performance-wise, this is where things get interesting, and it's a topic that comes up constantly in code review once a dataset grows past a few hundred items."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "membership_basics.py",
          "lines": [
            "fruits = ['apple', 'banana', 'mango']",
            "print('mango' in fruits)        # True",
            "print('grape' not in fruits)    # True",
            "",
            "text = \"Tech Sustainify\"",
            "print('Sustain' in text)        # True — substring check on strings",
            "",
            "user = {'name': 'Ashish', 'role': 'admin'}",
            "print('role' in user)           # True — checks KEYS, not values",
            "print('admin' in user)          # False — 'admin' is a value, not a key"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["True", "True", "True", "True", "False"]
        },
        {
          "type": "subHeading",
          "text": "The Performance Difference: List vs Set"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Checking membership in a <code>list</code> is O(n) — Python walks the list one item at a time until it finds a match or reaches the end. Checking membership in a <code>set</code> or <code>dict</code> is O(1) on average, because both are backed by hash tables. If you've got a list of ten thousand blocked user IDs and you're checking <code>if user_id in blocked_ids</code> inside a request handler that fires thousands of times a second, converting that list to a set once at startup is not a micro-optimization — it's the difference between your API responding in milliseconds and timing out under load. I've watched this exact fix cut a real endpoint's p99 latency dramatically, just by changing <code>blocked_ids = [...]</code> to <code>blocked_ids = set([...])</code>."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "membership_performance.py",
          "lines": [
            "# Slow for large data — O(n) lookup, walks the whole list",
            "blocked_ids = [101, 202, 303, 404, ...]   # imagine 50,000 entries",
            "if user_id in blocked_ids:",
            "    reject_request()",
            "",
            "# Fast — O(1) average lookup, hash-based",
            "blocked_ids = {101, 202, 303, 404, ...}   # same data, as a set",
            "if user_id in blocked_ids:",
            "    reject_request()"
          ]
        }
      ]
    },

    {
      "sectionId": "identity-operators",
      "title": "Identity Operators — is vs == (The One That Trips Everyone Up)",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "This is, without question, the single most misunderstood operator pair in the entire language. <code>==</code> checks value equality — do these two things have equal content? <code>is</code> checks identity — are these two names pointing at the literal same object in memory? They are not interchangeable, and Python won't warn you when you use the wrong one, because both are syntactically valid everywhere."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "is_vs_equals.py",
          "lines": [
            "a = [1, 2, 3]",
            "b = [1, 2, 3]",
            "c = a",
            "",
            "print(a == b)    # True  — same content",
            "print(a is b)    # False — different objects in memory",
            "print(a is c)    # True  — c points to the exact same list as a",
            "print(id(a), id(b), id(c))   # a and c share an id, b is different"
          ]
        },
        {
          "type": "outputBox",
          "lines": [
            "True",
            "False",
            "True",
            "140234871823424 140234871902784 140234871823424"
          ]
        },
        {
          "type": "subHeading",
          "text": "The CPython Small Integer Caching Trap"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Here's where it gets genuinely confusing, and this exact question shows up in almost every Indian service-company Python interview I've seen shared on forums. CPython caches small integers from -5 to 256 and short strings that look like identifiers, as an internal optimization. This means <code>a = 100; b = 100; a is b</code> gives <code>True</code>, purely by implementation accident — not because Python guarantees it. Try the same thing with <code>a = 1000; b = 1000</code> and <code>a is b</code> will almost always be <code>False</code>, because 1000 falls outside the cached range and gets allocated as a fresh object each time."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "integer_caching_trap.py",
          "lines": [
            "a = 100",
            "b = 100",
            "print(a is b)    # True — small ints are cached by CPython",
            "",
            "x = 1000",
            "y = 1000",
            "print(x is y)    # False (usually) — outside the -5 to 256 cache range",
            "",
            "print(x == y)    # True — value is always equal regardless"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["True", "False", "True"]
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "The rule to actually remember: never rely on <code>is</code> for value comparison, ever, even if it happens to work in your test cases. It's an implementation detail of CPython, not a language guarantee — PyPy or Jython could cache differently or not at all. The one legitimate use of <code>is</code> in everyday code is comparing against <code>None</code>, <code>True</code>, and <code>False</code>, because these are proper singletons in Python — there is exactly one <code>None</code> object in the entire running program."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "none_comparison.py",
          "lines": [
            "value = None",
            "",
            "# Correct — PEP 8 recommended:",
            "if value is None:",
            "    print(\"empty\")",
            "",
            "# Works but not idiomatic, and can behave oddly for custom __eq__ overrides:",
            "if value == None:",
            "    print(\"empty\")"
          ]
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "The second form isn't wrong exactly, but if a class overrides <code>__eq__</code> in a way that returns something unexpected when compared to <code>None</code>, <code>==</code> can misbehave in ways <code>is</code> never will, since <code>is</code> can't be overridden. PEP 8 explicitly recommends <code>is None</code> and <code>is not None</code> for this reason, and it's one of those linting rules that Pylint and flake8 will actually flag if you break it."
        }
      ]
    },

    {
      "sectionId": "operator-precedence",
      "title": "Operator Precedence — What Runs First",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Python evaluates expressions using a strict precedence order, same idea as PEMDAS/BODMAS from school math class, just with a lot more operators added to the queue. You don't need to memorize this table cell by cell — nobody does — but you need to know where the common ambiguity spots are, because that's where real bugs hide, usually in a conditional that looks correct at a glance."
        },
        {
          "type": "comparisonTable",
          "headers": ["Precedence (High to Low)", "Operators", "Description"],
          "rows": [
            ["1", "()", "Parentheses — always evaluated first"],
            ["2", "**", "Exponentiation (right-to-left associative)"],
            ["3", "~x, +x, -x", "Unary bitwise NOT, unary plus/minus"],
            ["4", "*, /, //, %", "Multiplication, division, floor division, modulus"],
            ["5", "+, -", "Addition, subtraction"],
            ["6", "<<, >>", "Bitwise shifts"],
            ["7", "&", "Bitwise AND"],
            ["8", "^", "Bitwise XOR"],
            ["9", "|", "Bitwise OR"],
            ["10", "==, !=, >, <, >=, <=, is, is not, in, not in", "Comparisons, identity, membership"],
            ["11", "not", "Logical NOT"],
            ["12", "and", "Logical AND"],
            ["13", "or", "Logical OR (lowest priority)"]
          ]
        },
        {
          "type": "subHeading",
          "text": "The Bug: Mixing 'and'/'or' Without Parentheses"
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Since <code>and</code> binds tighter than <code>or</code>, a condition like <code>a or b and c</code> is actually evaluated as <code>a or (b and c)</code>, not <code>(a or b) and c</code>. This is exactly the kind of thing that reads fine in a code review and then behaves unexpectedly three weeks later when someone adds a new condition without realizing the implicit grouping. If your condition has more than two logical operators mixed together, just add explicit parentheses. It costs you two characters and saves the next person (possibly future you) from re-deriving precedence rules under deadline pressure."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "precedence_bug.py",
          "lines": [
            "is_admin = False",
            "is_owner = True",
            "has_active_subscription = False",
            "",
            "# Looks like: (admin OR owner) AND has active subscription",
            "# Actually evaluates as: admin OR (owner AND has_active_subscription)",
            "can_access = is_admin or is_owner and has_active_subscription",
            "print(can_access)   # False — probably not what the author intended",
            "",
            "# Fix with explicit parentheses:",
            "can_access = (is_admin or is_owner) and has_active_subscription",
            "print(can_access)   # False — same result here, but now unambiguous and correct by design"
          ]
        },
        {
          "type": "outputBox",
          "lines": ["False", "False"]
        }
      ]
    },

    {
      "sectionId": "operators-vs-other-languages",
      "title": "Python Operators vs Java and JavaScript",
      "blocks": [
        {
          "type": "paragraph",
          "text": "If you're moving between languages regularly, this table saves you from the classic 'wait, why doesn't && work' moment during a live coding round."
        },
        {
          "type": "comparisonTable",
          "headers": ["Concept", "Python", "Java", "JavaScript"],
          "rows": [
            ["Logical AND", "and", "&&", "&&"],
            ["Logical OR", "or", "||", "||"],
            ["Logical NOT", "not", "!", "!"],
            ["Integer Division", "//", "/ (auto for two ints)", "Math.floor(a / b)"],
            ["Exponentiation", "**", "Math.pow(a, b)", "**"],
            ["Equality", "==", "== or .equals()", "== or ==="],
            ["Identity Check", "is", "==  (for objects)", "==="],
            ["String Concatenation", "+", "+", "+"],
            ["Chained Comparison", "1 < x < 10 (valid)", "Not supported directly", "Not supported directly"],
            ["Ternary", "x if cond else y", "cond ? x : y", "cond ? x : y"]
          ]
        }
      ]
    },

    {
      "sectionId": "code-heading",
      "title": "Try It Yourself — Operator Playground",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Nothing beats actually running the code. Edit the snippet below and change the operators — swap <code>//</code> for <code>/</code>, flip <code>and</code> to <code>or</code>, try negative numbers with modulus — and watch what happens."
        },
        {
          "type": "codeBox",
          "lang": "🐍 Python",
          "label": "operators_demo.py",
          "lines": [
            "a, b = 17, -5",
            "",
            "print(f\"a + b = {a + b}\")",
            "print(f\"a // b = {a // b}\")",
            "print(f\"a % b = {a % b}\")",
            "print(f\"a is b: {a is b}\")",
            "print(f\"a in [10, 17, 25]: {a in [10, 17, 25]}\")"
          ]
        },
        {
          "type": "outputBox",
          "lines": [
            "a + b = 12",
            "a // b = -4",
            "a % b = -3",
            "a is b: False",
            "a in [10, 17, 25]: True"
          ]
        },
        {
          "type": "subHeading",
          "text": "Practice This Code — Live Editor"
        },
        {
          "type": "liveEditor",
          "initialCode": "a, b = 17, -5\nprint(f\"a // b = {a // b}\")\nprint(f\"a % b = {a % b}\")"
        }
      ]
    },

    {
      "sectionId": "real-world-uses",
      "title": "Where Each Operator Actually Shows Up in Real Codebases",
      "blocks": [
        {
          "type": "paragraph",
          "text": "Operators aren't just interview fodder — here's where you'll genuinely run into each category once you're working on real projects, not just tutorial exercises."
        },
        {
          "type": "bulletList",
          "items": [
            { "strong": "Arithmetic in pagination logic", "text": " — total_pages = (total_items + page_size - 1) // page_size is the standard 'ceiling division' trick you'll write dozens of times across any project with paginated APIs." },
            { "strong": "Comparison in data validation", "text": " — Django and FastAPI form validators lean heavily on chained comparisons like 0 <= discount_percent <= 100 before accepting user input." },
            { "strong": "Assignment shortcuts in counters", "text": " — request_count += 1 inside rate-limiter middleware, running on every single API hit." },
            { "strong": "Logical operators in guard clauses", "text": " — if not user or not user.is_active: return HttpResponseForbidden() is one of the most common lines in any Django view." },
            { "strong": "Bitwise flags in Django's own ORM", "text": " — Django's field lookups and permission systems internally use bitmasking for certain performance-sensitive operations, and file permission handling (os.chmod) is pure bitwise arithmetic." },
            { "strong": "Membership checks in access control", "text": " — if role in ('admin', 'moderator', 'owner') gates almost every permission check you'll ever write." },
            { "strong": "Identity checks for singleton config objects", "text": " — if settings.DEBUG is True — checking against the actual singleton, not just a truthy value, matters when the config value could theoretically be something like 1 instead of True." }
          ]
        }
      ]
    },

    {
      "sectionId": "best-practices",
      "title": "Do's and Don'ts With Python Operators",
      "blocks": [
        {
          "type": "paragraph",
          "text": "A quick reference for the habits that separate code that works from code that works AND doesn't break during a code review or an on-call rotation."
        },
        {
          "type": "prosConsTable",
          "pros": [
            { "title": "Use is/is not only for None, True, False", "text": "These are true singletons in CPython. Everything else should use == for value comparison." },
            { "title": "Prefer sets over lists for repeated membership checks", "text": "Any if x in collection that runs inside a loop or a hot request path deserves a set, not a list." },
            { "title": "Parenthesize mixed and/or expressions", "text": "Don't rely on precedence memory during a code review six months from now. Make grouping explicit." },
            { "title": "Use chained comparisons where they read naturally", "text": "0 <= x < 100 is clearer and faster than 0 <= x and x < 100." },
            { "title": "Reach for math.isclose() with floats", "text": "Never compare floating-point results with == when precision matters, especially in financial or scientific code." },
            { "title": "Use the walrus operator only where it removes real duplication", "text": "While-loops reading input and filtered comprehensions are the sweet spot — not everywhere." }
          ],
          "cons": [
            { "title": "Don't use is to compare numbers or strings for value equality", "text": "CPython's integer/string caching is an implementation detail, not a guarantee — your code can silently break on a different Python build." },
            { "title": "Don't assume // truncates toward zero", "text": "It floors toward negative infinity. Test explicitly if your logic touches negative numbers." },
            { "title": "Don't mix bitwise operators into boolean logic by accident", "text": "& and | do NOT short-circuit like and/or do — both sides always get evaluated, which matters if one side has side effects." },
            { "title": "Don't compare across incompatible types and expect graceful handling", "text": "Python 3 raises a TypeError for ordering comparisons between unrelated types — write validation, don't rely on silent coercion." },
            { "title": "Don't overuse the walrus operator for style points", "text": "If it makes the line harder to read at a glance, you've misused it." }
          ]
        }
      ]
    },

    {
      "sectionId": "interview-questions",
      "title": "Python Operators — Interview Questions",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "These come up constantly, whether it's a fresher round at an Indian service company or a mid-level screen at a product startup. Know the reasoning behind the answer, not just the answer itself — interviewers almost always ask a follow-up 'why'."
        },
        {
          "type": "interviewQA",
          "items": [
            {
              "q": "Q1. What is the difference between == and is in Python?",
              "a": "== compares values — it checks whether two objects hold equal content. is compares identity — it checks whether two variable names point to the exact same object in memory (same id()). Two lists with identical contents will be == True but is False if they are separate objects. is should only be used reliably for None, True, and False comparisons.",
              "tag": "Must Know"
            },
            {
              "q": "Q2. Why does -7 // 2 return -4 instead of -3 in Python?",
              "a": "Python's floor division always rounds toward negative infinity, not toward zero. -7 / 2 is -3.5, and flooring -3.5 (rounding down) gives -4. This differs from C or Java, where integer division truncates toward zero, giving -3. If you need truncation behavior, use math.trunc(a / b) instead of //.",
              "tag": "Frequently Asked"
            },
            {
              "q": "Q3. Why does 0.1 + 0.2 == 0.3 return False in Python?",
              "a": "This is a floating-point precision issue rooted in IEEE 754 binary representation, present in nearly every language, not specific to Python. 0.1 and 0.2 cannot be represented exactly in binary floating point, so their sum is actually 0.30000000000000004. Use math.isclose(0.1 + 0.2, 0.3) or the decimal module for precise comparisons.",
              "tag": "Common Trap"
            },
            {
              "q": "Q4. What does the walrus operator (:=) do?",
              "a": "Introduced in Python 3.8 (PEP 572), the walrus operator assigns a value to a variable as part of a larger expression, letting you both compute and use a value in one line. Common in while-loop conditions and list comprehensions where you'd otherwise need to call the same expensive expression twice.",
              "tag": "Frequently Asked"
            },
            {
              "q": "Q5. Why can a is b return True for a = 100, b = 100 but False for a = 1000, b = 1000?",
              "a": "CPython caches small integers in the range -5 to 256 as a performance optimization, so variables assigned the same small integer point to the same cached object. Integers outside that range are allocated freshly, so a is b usually returns False even though a == b is True. This is a CPython implementation detail, not a language guarantee.",
              "tag": "Common Trap"
            },
            {
              "q": "Q6. What is short-circuit evaluation in Python's logical operators?",
              "a": "and and or stop evaluating as soon as the result is determined. and returns the first falsy value without checking the rest; or returns the first truthy value without checking the rest. This is used deliberately to avoid errors, like checking user and user.profile before accessing user.profile.email, which prevents an AttributeError when user is None.",
              "tag": "Must Know"
            },
            {
              "q": "Q7. What's the difference between & and and in Python?",
              "a": "and is a logical operator working on truthy/falsy values and short-circuits. & is a bitwise operator that operates on the binary representation of integers (or performs set intersection when used on sets) and does not short-circuit — both operands are always evaluated. Using & where you meant and is a common source of bugs, especially in pandas filtering expressions where & is required instead of and due to how pandas overloads operators.",
              "tag": "Frequently Asked"
            },
            {
              "q": "Q8. Why is checking membership in a set faster than in a list?",
              "a": "A list stores items sequentially, so checking membership (x in my_list) requires Python to scan items one at a time in the worst case — O(n) time complexity. A set is backed by a hash table, so membership checks compute a hash and jump nearly directly to the right bucket — O(1) average time complexity. For large collections checked repeatedly, converting to a set gives a substantial performance improvement.",
              "tag": "Must Know"
            },
            {
              "q": "Q9. What is the result of chaining comparisons like 1 < 2 < 3 in Python?",
              "a": "It evaluates to True, and Python treats it as (1 < 2) and (2 < 3), evaluating each adjacent pair and combining with logical AND, while only evaluating each middle expression once. This is different from most C-family languages, where 1 < 2 < 3 would first evaluate 1 < 2 to True (1), then compare 1 < 3, which is also True but for an entirely different, coincidental reason.",
              "tag": "Common Trap"
            },
            {
              "q": "Q10. Does the += operator behave the same for integers and lists?",
              "a": "No. For immutable types like integers, x += 1 creates a new object and rebinds x to it, identical to x = x + 1. For mutable types like lists, a += [1] mutates the existing list object in place using __iadd__, so any other variable referencing the same list will see the change too. a = a + [1] instead creates a brand new list, leaving other references untouched.",
              "tag": "Frequently Asked"
            }
          ]
        }
      ]
    },

    {
      "sectionId": "practice-questions",
      "title": "Practice Questions — Test Your Understanding",
      "blocks": [
        {
          "type": "paragraph",
          "text": "Try to answer each one mentally before checking. If you get one wrong, that's exactly the kind of gap that turns into a real bug later — worth re-reading that section above."
        },
        {
          "type": "practiceQuestions",
          "items": [
            {
              "q": "1. What does print(10 % -3) output, and why?",
              "a": "It outputs -2. Python guarantees a == (a // b) * b + (a % b). 10 // -3 is -4 (floors toward negative infinity), and -4 * -3 = 12. So a % b must be 10 - 12 = -2 to satisfy the identity. The sign of the modulus result always matches the sign of the divisor in Python.",
              "difficulty": "Medium"
            },
            {
              "q": "2. What is the output of print([1, 2] == [1, 2]) and print([1, 2] is [1, 2])?",
              "a": "True and False. == compares contents, and both lists have identical elements, so it's True. is compares object identity — these are two separately constructed list objects with different memory addresses, even though their contents match, so it's False.",
              "difficulty": "Easy"
            },
            {
              "q": "3. What happens when you run 'abc' > 5 in Python 3?",
              "a": "It raises TypeError: '>' not supported between instances of 'str' and 'int'. Python 3 disallows ordering comparisons between fundamentally unrelated types, unlike Python 2, which allowed it and produced an arbitrary but consistent result based on type name.",
              "difficulty": "Easy"
            },
            {
              "q": "4. Why might x = x & y behave completely differently from x = x and y when x and y are both integers?",
              "a": "and is logical and returns one of the actual operand values based on truthiness (short-circuiting). & is bitwise AND and operates on the binary digits of both integers regardless of truthiness, always evaluating both sides. For example, 5 and 3 returns 3 (the second truthy operand), while 5 & 3 returns 1 (the bitwise AND of 0101 and 0011).",
              "difficulty": "Hard"
            },
            {
              "q": "5. What's the result of (True + True) in Python, and why does it work at all?",
              "a": "It's 2. In Python, bool is a subclass of int, where True behaves as 1 and False as 0 in numeric contexts. This is why you can sum a list of booleans to count how many are True, e.g., sum([True, False, True, True]) gives 3.",
              "difficulty": "Medium"
            },
            {
              "q": "6. What does 'a' in {'a': 1, 'b': 2} check — the keys or the values?",
              "a": "It checks the keys. For dictionaries, the in operator only checks membership against keys by default. To check values, you need 1 in {'a': 1, 'b': 2}.values() explicitly.",
              "difficulty": "Easy"
            },
            {
              "q": "7. Why does (n := 5) work inside a list comprehension condition but assigning with = doesn't?",
              "a": "Regular = is a statement in Python, not an expression, so it cannot legally appear inside an expression context like a comprehension's condition. The walrus operator := was specifically introduced as an assignment expression — it both assigns and evaluates to a value — making it valid wherever an expression is expected, which a plain = assignment is not.",
              "difficulty": "Hard"
            },
            {
              "q": "8. What's the output of print(bool('False'))?",
              "a": "True. Any non-empty string is truthy in Python, regardless of its content — even the literal string 'False' is a non-empty string and therefore truthy. Only an actual empty string '' is falsy. This trips up beginners who assume the string content itself is being evaluated as a boolean expression, which it is not.",
              "difficulty": "Medium"
            }
          ]
        }
      ]
    },

    {
      "sectionId": "conclusion-heading",
      "title": "Conclusion — Operators Are Simple Until They Aren't",
      "blocks": [
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Operators are the first thing every Python tutorial teaches, and the last thing most developers actually master. That's not a contradiction — it's just how it goes. Arithmetic and comparison operators feel obvious because they map directly onto math you already know. The real learning curve is everywhere else: floor division with negative numbers, <code>is</code> versus <code>==</code>, short-circuit evaluation as an actual control-flow tool rather than a performance footnote, and the quiet but important difference between <code>+=</code> on a list versus <code>+=</code> on an integer."
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "If there's one habit worth building from this entire guide, it's this: whenever you write a comparison or a boolean expression involving more than one operator, pause for two seconds and ask what precedence rule is actually running underneath it. That two-second pause is cheaper than the debugging session you'll otherwise have three sprints from now, when this exact line of code is buried inside a much bigger function and nobody remembers why it was written that way."
        },
        {
          "type": "comparisonTable",
          "headers": ["Operator Category", "Master This First If You're..."],
          "rows": [
            ["Arithmetic + Comparison", "Completely new to programming"],
            ["Logical + Assignment", "Writing conditionals and loops daily"],
            ["Identity (is vs ==)", "Preparing for interviews or debugging weird equality bugs"],
            ["Membership", "Working with large datasets or filtering logic"],
            ["Bitwise", "Doing systems programming, networking, or performance-critical code"]
          ]
        },
        {
          "type": "paragraph",
          "isHtml": true,
          "text": "Next, take these operators into actual control-flow structures — <code>if</code>/<code>elif</code>/<code>else</code>, loops, and comprehensions — where they get combined in ways that reveal exactly why precedence and short-circuiting matter in practice, not just in theory. That's where operators stop being syntax you memorized and start being tools you actually reach for without thinking."
        }
      ]
    }

  ],

  "faq": {
    "sectionId": "faq-heading",
    "title": "Frequently Asked Questions (FAQ)",
    "items": [
      {
        "q": "What is the difference between / and // in Python?",
        "a": "/ is true division and always returns a float, even when the numbers divide evenly (10 / 2 gives 5.0). // is floor division and returns the largest integer less than or equal to the exact result, rounding toward negative infinity — not toward zero. For negative numbers this distinction matters a lot: -7 // 2 gives -4, not -3."
      },
      {
        "q": "Can you use ++ or -- in Python like in Java or C?",
        "a": "No, Python does not have increment (++) or decrement (--) operators. Writing ++x is legal syntax but does nothing useful — it's just unary plus applied twice. Use x += 1 or x -= 1 instead, which is the idiomatic and only correct way to increment or decrement a variable in Python."
      },
      {
        "q": "Is Python's 'and'/'or' the same as '&&'/'||' in other languages?",
        "a": "Functionally similar — both short-circuit — but not identical in behavior. Python's and/or return one of the actual operand values (not necessarily a boolean), while && and || in C-family languages typically coerce and return a strict boolean. Python also uses full English words instead of symbols, which is a deliberate readability choice by the language designers."
      },
      {
        "q": "Why does 'is' sometimes work for comparing integers and sometimes doesn't?",
        "a": "CPython caches small integers between -5 and 256 as a memory optimization, so two variables holding the same small integer often point to the same cached object, making 'is' return True. Outside that range, integers are typically created as fresh objects each time, so 'is' usually returns False even for equal values. Never rely on this — always use == for value comparison."
      },
      {
        "q": "Are Python operators overloaded for custom classes?",
        "a": "Yes. Python lets you define custom behavior for operators on your own classes using dunder methods like __add__ for +, __eq__ for ==, __lt__ for <, and so on. This is how libraries like NumPy and Pandas make + and & work meaningfully on arrays and DataFrames instead of just plain numbers."
      },
      {
        "q": "What's the difference between & and and when filtering a Pandas DataFrame?",
        "a": "In Pandas, you must use & (bitwise AND) instead of and (logical AND) when combining boolean conditions on Series, like df[(df['age'] > 18) & (df['active'] == True)]. This is because and expects single boolean values and can't handle element-wise operations across an entire Series, while & is overloaded by Pandas to work element-wise across arrays. Forgetting the parentheses around each condition when using & is also a very common source of a ValueError."
      }
    ]
  },

  "nav": {
    "prev": { "label": "← Previous: Python Type Casting", "path": "/python/python-type-casting" },
    "next": { "label": "Next: Python User Input →", "path": "/python/python-user-input" }
  },

  "sidebar": {
    "quickFacts": [
      { "dt": "Operator Categories", "dd": "7 (Arithmetic, Comparison, Assignment, Logical, Bitwise, Membership, Identity)" },
      { "dt": "Total Symbols/Keywords", "dd": "30+" },
      { "dt": "Newest Addition",       "dd": "Walrus operator := (Python 3.8, PEP 572)" },
      { "dt": "Trickiest Operator",   "dd": "is (identity, not equality)" },
      { "dt": "Precedence Levels",    "dd": "13" },
      { "dt": "Related Module",      "dd": "operator (functional equivalents of all operators)" }
    ],
    "relatedArticles": [
      { "label": "Python Variables",     "path": "/python/python-variables" },
      { "label": "Python Data Types",    "path": "/python/python-datatypes" },
      { "label": "Python Control Flow",  "path": "/python/python-controlflow" },
      { "label": "Python Functions",     "path": "/python/python-functions" },
      { "label": "Python Loops",         "path": "/python/python-loops" },
      { "label": "Python OOP",           "path": "/python/python-oop" },
      { "label": "Python Sets",          "path": "/python/python-sets" },
      { "label": "Python Dictionaries",  "path": "/python/python-dictionaries" }
    ],
    "tags": ["Arithmetic Operators","Comparison Operators","Logical Operators","Bitwise Operators","Assignment Operators","Membership Operators","Identity Operators","Walrus Operator","Operator Precedence","is vs ==","Floor Division","Short-Circuit Evaluation"]
  }
}